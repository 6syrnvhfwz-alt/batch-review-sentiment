# 第2周 Day 1 学习指南

## 今天的目标
把一个 CSV 文件里的 35 条电商评价，批量交给大模型判断情感，把结果写回文件，再做统计汇总。
这就是一个典型的「AI 落地降本增效」小项目——你之前做单条分析，今天做批量。

## 本地电脑设置（先做这步）

打开 PowerShell，执行：
```powershell
mkdir D:\ai_learning\week02_batch_review
cd D:\ai_learning\week02_batch_review
```

然后把云端生成的 2 个文件拷过来（或者直接在本地新建同名文件复制内容）：
- `评价数据.csv`
- `batch_sentiment.py`（这是今天的脚手架，你所有代码都写在这）

## 学习顺序（一步一步来，不要跳步）

### Step 1：先跑通任务1（文件读取）—— 30分钟
1. 用 VSCode 打开 `batch_sentiment.py`
2. 找到 `任务1` 的代码区，实现 `load_reviews()` 函数
3. 跑命令看结果：
   ```powershell
   python batch_sentiment.py
   ```
4. 应该看到：
   ```
   === 任务1：读取 CSV ===
   共读取 35 条评价
   前3条：
   {'id': '1', '评价内容': '衣服质量很好...'}
   ...
   ```

**任务1 跑通了再做下一步。** 卡住了来问我。

### Step 2：先手动调一次 API（任务2 的核心）—— 30分钟
先不要写进函数里，先单独写一个小脚本试试：

新建 `test_api.py`，写：
```python
import requests

url = 'http://localhost:11434/api/generate'
data = {
    'model': 'qwen2:7b',
    'prompt': '对这条电商评价做情感分析，返回JSON：{"情感":"正面/负面/中性","理由":"一句话"}\n评价：衣服质量很好，穿着很舒服',
    'stream': False
}

r = requests.post(url, json=data)
print(r.json()['response'])
```

跑一下 `python test_api.py`，看看模型返回什么。

**关键学习点**：
- `requests.post(url, json=data)` —— 这是 Python 调 HTTP 接口的标准写法
- `r.json()` —— 把返回的字符串解析成字典
- 看返回的 `response` 字段里是不是你要的 JSON

**注意**：Ollama 返回的 response 是字符串，里面可能包了 markdown 代码块 ```json ```，需要清理一下再 json.loads()。

### Step 3：把任务2封装成函数
把上面跑通的代码封装进 `analyze_sentiment()` 函数，加 try/except 防止一条错全挂。

### Step 4：任务3 + 4 + 5 串联（1-2小时）
按顺序写，每完成一个任务就运行一次看结果。

## 关键代码片段速查

### 读 CSV
```python
import csv

with open('评价数据.csv', 'r', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for row in reader:
        print(row['id'], row['评价内容'])
```

### 调 Ollama API
```python
import requests, json

url = 'http://localhost:11434/api/generate'
body = {'model': 'qwen2:7b', 'prompt': '...', 'stream': False}
r = requests.post(url, json=body)
text = r.json()['response']
# 清理 markdown 代码块
text = text.strip().strip('`').replace('json', '').strip()
result = json.loads(text)
```

### 写 CSV
```python
import csv

with open('输出.csv', 'w', encoding='utf-8-sig', newline='') as f:
    writer = csv.DictWriter(f, fieldnames=['id', '评价内容', '情感', '理由'])
    writer.writeheader()
    for item in results:
        writer.writerow(item)
```

### 统计
```python
counts = {'正面': 0, '负面': 0, '中性': 0}
for r in results:
    counts[r['情感']] += 1
total = len(results)
for k, v in counts.items():
    print(f'{k}: {v} 条 ({v/total*100:.1f}%)')
```

## 常见坑
1. **CSV 编码**：Excel 打开中文 CSV 会乱码，用 `utf-8-sig` 保存就不会
2. **大模型返回格式不稳**：偶尔返回的不是 JSON，要 try/except 捕获
3. **Ollama 服务没启动**：先 `ollama list` 看看模型在不在
4. **文件路径问题**：确保脚本和 CSV 在同一个目录

## 完成标志
- [ ] `评价分析结果.csv` 生成了 35 行数据
- [ ] 控制台打印出正面/负面/中性的数量和占比
- [ ] `负面评价汇总.txt` 里只有负面评价
- [ ] 用 `git add / git commit` 提交到你的 GitHub 仓库

## 时间分配（6小时参考）
- 设置本地项目 + 看材料：30分钟
- 任务1（读文件）：30分钟
- 任务2（调API）：1-1.5小时（含踩坑）
- 任务3（批量+写文件）：1-1.5小时
- 任务4+5（统计+导出）：1小时
- 调试 + 提交 GitHub：1小时
